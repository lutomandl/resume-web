export type LanguageEnum = 'en' | 'de' | 'cs';

export type MenuItems = {
  about: string;
  contact: string;
  //   experience: string;
  //   projects: string;
};

export type ContactFormValues = {
  name: string;
  email: string;
  message: string;
};
